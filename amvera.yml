
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: uvicorn app.api.main:app --host 127.0.0.1 --port 8000
  containerPort: 8000
  persistenceMount: /data

